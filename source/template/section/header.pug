header.header
  .header__top
    .container
      .contacts
        a.contact(href="+30777777777")
          i.fa.fa-mobile(aria-hidden="true")
          span.contact__text +30777777777

        a.contact(href="mailto:info@business.co")
          i.fa.fa-envelope.contact__icon.contact__icon--email(aria-hidden="true")
          span.contact__text info@business.co
          
        a.facebook-icon(href="facebook.com")
          i.fa.fa-facebook(aria-hidden="true")
  .header__bottom
    .container
      .logo: img.logo__picture(src="img/logo.png", alt="picture")
      nav.nav
      - var items = ['Home', 'About us', 'Our Approch', 'Our Work']
        each item in items
          if item === 'Home'
            a.nav__link.nav__link--active(href='#')=item
          else
            a.nav__link(href='#')=item
      a.button.header__button.button--red(href='#') Free Consultation
        